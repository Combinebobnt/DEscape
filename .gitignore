.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
examples/
config.yaml

# Claude Code harness state, not project source
.claude/
.mcp.json

# Cython build output (native/iso_render/) -- generated by setup.py
# build_ext, not source
native/iso_render/build/
native/iso_render/*.c
native/iso_render/*.so
native/iso_render/*.html

# Native composite kernel build output (tools/build_native.py), and its
# build/failed stamps -- generated, not source
descape/_composite_native*.c
descape/_composite_native*.so
descape/_composite_native*.pyd
descape/_composite_native*.html
descape/_composite_native.stamp
descape/_composite_native.failed

# tools/verify_iso_render.py's reference PNGs -- large, regenerated by
# that script, not source
build/

# PyInstaller output (packaging/descape.spec) -- generated, not source
dist/

# Private maintainer-only planning docs, own git repo, not this one's
/maintainer/
